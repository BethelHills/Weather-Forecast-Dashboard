{
  "buildCommand": "cd weather-app && npm install && npm run build",
  "outputDirectory": "weather-app/dist",
  "installCommand": "cd weather-app && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
